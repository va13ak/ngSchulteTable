import {Environment} from "./interface";

export const environment : Environment  = {
  tableCols: 5, tableRows: 5,
  tableWidth: '600px', tableHeight: '600px',
  statusBarHeight: '50px',
  production: true
};
